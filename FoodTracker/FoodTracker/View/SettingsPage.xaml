<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FoodTracker.View.SettingsPage"
             Title="Settings">
    <Grid>
        <!--<Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackLayout Orientation="Horizontal" >
            <Label Text="Interval: " Margin="20,0,20,0" VerticalOptions="Center"/>
            <TimePicker x:Name="picker" />
        </StackLayout>-->
        <!--<ListView x:Name="listView" ItemTapped="listView_ItemTapped" ItemSelected="listView_ItemSelected">
            <ListView.ItemTemplate>
                <DataTemplate>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>-->
        <TableView x:Name="tableView" Intent="Settings">
            <TableRoot>
                <TableSection Title="Basics">
                    <ViewCell>
                        <StackLayout Orientation="Horizontal" Padding="13,0">
                            <Label Text="Interval" VerticalOptions="Center" Margin="5,0,0,0"/>
                            <TimePicker Time="0:00" HorizontalOptions="EndAndExpand"/>
                        </StackLayout>
                    </ViewCell>
                </TableSection>
            </TableRoot>
        </TableView>
    </Grid>
</ContentPage>